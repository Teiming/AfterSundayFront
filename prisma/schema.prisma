generator client {
  provider = "prisma-client"
  output   = "../lib/prisma/generated"
  binaryTargets = ["native", "rhel-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  schemas  = ["auth", "etc", "event"]
}